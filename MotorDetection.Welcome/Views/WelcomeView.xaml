<UserControl x:Class="MotorDetection.Welcome.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MotorDetection.Welcome.Views"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:cm="http://caliburnmicro.com"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Grid.Column="0" TextAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="24"
                       Text="{Binding Text}">
            </TextBlock>
            <Button Content="测试" x:Name="Test">
                <!--<b:Interaction.Triggers>
                    <b:EventTrigger EventName="Click">
                        <cm:ActionMessage MethodName="Test" />
                    </b:EventTrigger>
                </b:Interaction.Triggers>-->
            </Button>

        </StackPanel>

    </Grid>
</UserControl>
